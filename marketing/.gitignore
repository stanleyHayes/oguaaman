node_modules
dist
*.tsbuildinfo
.env
.env.*
# Public build-time config (URLs only, no secrets) — committed so the deployed
# build is self-contained. Vite ships VITE_* to the client anyway.
!.env.production
!.env.example
.DS_Store
