# Oguaa admin SPA — environment variables.
# Copy to .env.local for development, fill values in your deployment platform for production.

# Cloudinary unsigned image upload (Newsroom cover images, institution editors).
# Create an UNSIGNED upload preset in your Cloudinary dashboard, then set:
VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=your-unsigned-preset

# API origin (defaults to same-origin /api via the Vite proxy in dev).
# Production: the API origin. REQUIRED on Vercel (no same-origin /api proxy).
# VITE_API_URL=https://api.oguaaman.com
VITE_API_URL=
